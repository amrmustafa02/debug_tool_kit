name: debug_toolkit_example
description: Example app demonstrating debug_toolkit usage.

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.0.0
  debug_toolkit:
    path: ../

flutter:
  uses-material-design: true
